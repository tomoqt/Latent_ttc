Write a detailed, step-by-step explanation of how transformers process text, starting from tokenization through embeddings, positional encodings, multi-head self-attention, residual connections, feedforward layers, layer normalization, and decoding, including a concrete toy example with tiny tensors (e.g., 3 tokens, 2 heads, 4-dim embeddings), and discuss training dynamics (loss functions, teacher forcing, gradient flow), optimization (AdamW, learning rate schedules, weight decay), regularization (dropout, label smoothing), inference-time behaviors (greedy, beam search, nucleus sampling), evaluation metrics (perplexity, accuracy), and practical pitfalls when scaling models (context length limits, instability, data contamination), concluding with a checklist of best practices for reproducible experiments.